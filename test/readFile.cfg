process TEST = {
    
    source = DaqSource  {
        untracked string readerPluginName = "DTDDUFileReader"
	untracked PSet readerPset = { 
	    untracked string fileName ="rawFileName.i0"
	    untracked bool isRaw = true
       	}
    }

    module dump = DumpFEDRawDataProduct{ }
    
    module out = PoolOutputModule {
	untracked string fileName ="rawdata.root"
    }
    
    path p = {dump}
    endpath ep={out}
}
