process TEST = {
	source = DaqSource{ string reader = "DTDDUFileReader"
        	untracked int32 maxEvents = 10
		PSet pset = { string fileName ="ddu.raw" } 
	}

	module dump = DumpFEDRawDataProduct{ }

	module out = PoolOutputModule {
        	untracked string fileName ="rawdata.root"
	}
	 
	path p = {dump}
        endpath ep={out}
}
